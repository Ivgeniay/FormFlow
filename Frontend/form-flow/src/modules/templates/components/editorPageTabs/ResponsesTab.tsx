import { TemplateDto } from "../../../../shared/api_types";

export interface TabProps {
	template: TemplateDto;
	accessToken: string | null;
}

export const ResponsesTab: React.FC<TabProps> = ({}) => {
	return <></>;
};
